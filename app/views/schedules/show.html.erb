<%= render @schedule %>

<div>
  <%= link_to "Edit this schedule", edit_schedule_path(@schedule) %> 
  <%= link_to "Back to schedules", schedules_path %>

  <%= button_to "Destroy this schedule", @schedule, form: { data: { 'turbo-confirm': 'Are you sure?' } }, method: :delete %>
</div>
